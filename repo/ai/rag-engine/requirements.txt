fastapi==0.139.0
uvicorn[standard]==0.29.0
pydantic-settings==2.14.2
httpx==0.28.1
opentelemetry-sdk==1.26.0
opentelemetry-instrumentation-fastapi==0.47b0
# LlamaIndex minimal: only SentenceSplitter (chunk_service) and DoclingReader
# (parse_service Word/Excel/PPT). All other LlamaIndex packages removed in
# Issue #039 (RAG refactor Step 11 — delete old path).
llama-index-core==0.14.23
llama-index-readers-docling==0.4.2
# Embedding model is served by the AI inference service (vLLM/sglang OpenAI
# compatible /v1/embeddings). We use a thin custom adapter backed by the
# ``openai`` SDK rather than llama-index-embeddings-openai.
openai>=1.50.0
# PyMuPDF for lightweight PDF text + image extraction (no model download).
pymupdf>=1.24.0
# Office document parsing (Word/Excel/PPT image extraction in parse_service +
# server.py). Previously transitive deps of the full llama-index package.
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
# gRPC server (Parse/Embed/Generate/GenerateStream RPCs) + internal rag.proto
# generated stubs.
grpcio>=1.62.0
grpcio-tools>=1.62.0
